import { spacing } from "../../../../styling";

const originalWidth = 350;
const originalHeight = 300;
export const aspectRatio = originalWidth / originalHeight;
export const width = spacing.componentWidth;
